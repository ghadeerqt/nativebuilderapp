PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_IDENTIFIER)$(BUNDLE_IDENTIFIER_APPEND)

BUNDLE_DISPLAY_NAME = NativeBuilderApp
BUNDLE_IDENTIFIER = com.nativebuilderapp.mobile

RUNTIME_URL = http:\/\/localhost:8080

PRODUCT_NAME = NativeBuilderAp
